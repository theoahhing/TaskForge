/*
    Log Level defines the supported logging severity levels used by the
    application when writing diagnostic and runtime messages.
*/

namespace TaskForge.App.Models;

public enum LogLevel
{
    Debug,
    Info,
    Warning,
    Error
}
